#include "PChecker.h"

//override the function "checkMyLetter"
//create the function definition here
	char checkMyLetter(char inputChar){char defaultLetter = inputChar;if(inputChar == 'p' || inputChar == 'P'){return 'p';}else{return defaultLetter;}}
