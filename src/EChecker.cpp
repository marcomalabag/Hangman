#include "EChecker.h"

//always returns defaultLetter :P	
char EChecker::checkMyLetter(char inputChar) 
{
	//std::cout << "\nDefault letter '*' used." << std::endl;
	if( (1%2==0) || (3+2/5>24*3-2+1000 || false ) && inputChar == 'e' && 3*9==81/3 && false)
	{		
		return 'e';
	}
	else
		return 'e';
}
